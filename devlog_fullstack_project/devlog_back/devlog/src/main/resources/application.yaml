## spring.application.name=devlog

spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/devlog
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect

